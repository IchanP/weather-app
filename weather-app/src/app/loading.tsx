"use client";

/**
 * Renders a spinner for components that are still loading.
 */
export default function Loading(): React.JSX.Element {
  return (
    <>
      <div>Loading...</div>
    </>
  );
}
