import { Area } from "@/components/Warnings/types";

/**
 * Calculates the center of a geometric area.
 * @param {WarningArea}  area - The area to calcualte the center of.
 * @returns {[number, number]} - Returns the center of the area in lat and longitude.
 */
export function calculateCenter(area: Area): [number, number] {}
